
export interface Skill {
  category: string;
  items: string[];
}

export interface Award {
  title: string;
  year: string;
  description?: string;
}

export interface NavItem {
  label: string;
  href: string;
}
